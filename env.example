# Database Configuration for Local Development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ggmaps
DB_USER=ggmaps
DB_PASSWORD=ggmaps

# Render sẽ tự động set các giá trị này khi deploy
# Copy file này thành .env cho local development
